# Dotfiles

This repo serves as an archive for the important configuration files for my Linux dev experience.

**To Clone:**
```bash
git clone --recurse-submodules https://github.com/abdallahsoliman00/dotfiles.git
```

Also, don't forget to change the nvim subrirectory's branch to `linux` from `main`.
